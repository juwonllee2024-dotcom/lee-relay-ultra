# Project Analysis

Before changing a project, inspect its root, package manager, package scripts,
README, Git status, and available test/build commands. Do not assume npm or a
framework. Record the detected project type and use its existing scripts.
